---
weight: 50
---

# FilterByBoolTag

Filter for sequences that have a boolean tag.

```toml
[[step]]
    action = "FilterByBoolTag"
    label = "mytag"
    keep_or_remove = "Keep" # or "Remove"
```

