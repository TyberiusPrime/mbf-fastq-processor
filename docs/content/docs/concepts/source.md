# Source

When a step refers to a 'source' (instead of a [`segment`]({{< relref "docs/concepts/segments.md" >}})),
it means that it can read from segment, a segment's name (e.g. "name:read1") or a (location/string) tag value
(e.g. "tag:mytag")


