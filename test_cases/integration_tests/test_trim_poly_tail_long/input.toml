[input]
    read1 = 'input_read1.fq'

[[step]]
    action = 'TrimPolyTail'
    min_length = 10
    target = 'Read1'
    base = 'A'
    max_mismatch_rate = 0.10
    max_consecutive_mismatches = 3

[output]
    prefix = 'output'