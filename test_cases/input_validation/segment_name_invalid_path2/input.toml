# Test case to verify that segment names cannot be empty in transformation steps
# This should fail validation when trying to reference an empty segment

[input]
"sh\\u" = 'input_read1.fq'

[options]
	accept_duplicate_files = true # not what we're testing

[output]
prefix = 'output'
compression = 'Raw'

