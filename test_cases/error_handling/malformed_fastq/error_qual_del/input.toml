[input]
    error_read = "input_error_qual_del.fastq"

[[step]]
	action = 'ValidateQuality'
	encoding = 'Sanger'

[output]
    prefix = "output"

# --- Output steps (migrated from [output]) ---
[[step]]
    action = "OutputFASTQ"
