[input]
    error_read = "input_error_qual_space.fastq"
[[step]]
	action = 'ValidateQuality'
	encoding = 'sanger'

[output]
    prefix = "output"

# --- Output steps (migrated from [output]) ---
[[step]]
    action = "OutputFASTQ"
