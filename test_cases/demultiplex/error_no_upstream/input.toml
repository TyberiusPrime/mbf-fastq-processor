[input]
    read1 = 'input_read1.fq'

[output]
    prefix = "output"
[[step]]
    action = 'Demultiplex'
    in_label = 'in_other_file'
    output_unmatched = false

# --- Output steps (migrated from [output]) ---
[[step]]
    action = "OutputFASTQ"
