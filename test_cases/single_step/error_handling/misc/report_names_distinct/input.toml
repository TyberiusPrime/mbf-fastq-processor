[input]
    read1 = 'sample_data/ten_reads_of_var_sizes.fq'

[[step]]
    action = 'Report'
    name = 'report'

[[step]]
    action = 'Report'
    name = 'report'

[output]
    prefix = "output"

# --- Output steps (migrated from [output]) ---
[[step]]
    action = "OutputFASTQ"

[[step]]
    action = "OutputReport"
    json = true
    html = false
