[input]
    read1 = 'sample_data/test_phred.fq'
[[step]]
    action = 'Rename'
    search = '['
    replacement = ';'

[output]
    prefix = "output"

# --- Output steps (migrated from [output]) ---
[[step]]
    action = "OutputFASTQ"
