[input]
    read1 = 'nonexistent_file.fq'

[output]
    prefix = "output"
[[step]]
    action = 'Report'
    name = 'start'

# --- Output steps (migrated from [output]) ---
[[step]]
    action = "OutputFASTQ"
    compression = "Raw"

[[step]]
    action = "OutputReport"
    json = true
    html = false
