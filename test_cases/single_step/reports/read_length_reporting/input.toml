[input]
    read1 = 'input_read1.fq'

[[step]]
    action = 'Report'
    name = 'report'
    count = false
    length_distribution = true

[output]
    prefix = "output"

# --- Output steps (migrated from [output]) ---
[[step]]
    action = "OutputReport"
    json = true
    html = false
