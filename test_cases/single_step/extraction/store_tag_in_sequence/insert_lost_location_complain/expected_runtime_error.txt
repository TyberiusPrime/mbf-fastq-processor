StoreTagInSequence(val_tag->pos_tag): the captured location of 1 read(s) was lost to edits applied after the tag was captured (rows [0]).
  For example, read 'Read1' had its tag anchored at 2 in a 12bp read, but the edits applied since (cut 10 from the start) removed or split those bases, leaving nowhere to write it back
