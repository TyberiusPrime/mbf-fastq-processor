[input]
    read1 = 'input_read1.fq'
[[step]]
    action = 'ValidateAllReadsSameLength'
    source = 'all'
[output]
    prefix = "output"

# --- Output steps (migrated from [output]) ---
[[step]]
    action = "OutputFASTQ"
