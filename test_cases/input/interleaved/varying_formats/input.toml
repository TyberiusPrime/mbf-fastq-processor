[input]
  r = ['input_interleaved.fq', 'input_interleaved.fasta']
  interleaved = ['read1', 'read2']
  options.fasta_fake_quality = 33

[options]
accept_duplicate_files = true

[output]
    prefix = "output"

# --- Output steps (migrated from [output]) ---
[[step]]
    action = "OutputFASTQ"
