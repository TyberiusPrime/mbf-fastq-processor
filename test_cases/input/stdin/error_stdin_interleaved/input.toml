[input]
    seq = ['input.fq', '--stdin--']
    interleaved = ['read1','read2']

[[step]]
    action = 'Head'
    n = 1

[output]
    prefix = "output"

# --- Output steps (migrated from [output]) ---
[[step]]
    action = "OutputFASTQ"
