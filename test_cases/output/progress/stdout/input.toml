[input]
    read1 = 'input_read1.fq'

[output]
    prefix = "output"
[[step]]
    action = 'Progress'
    n = 100000

[[step]]
  action = "EvaluateExpression"
  out_label = "nope"
  expression = "0"
  output_type = 'bool'

[[step]]
  action = "FilterByTag"
  in_label = "nope"
  keep_or_remove = "keep"

# --- Output steps (migrated from [output]) ---
[[step]]
    action = "OutputFASTQ"
