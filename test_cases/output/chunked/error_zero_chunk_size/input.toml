[input]
    read1 = ["input_read1.fq", "input_read1.fq"]

[options]
    accept_duplicate_files = true
	block_size=4

[output]
    prefix = "output"

# --- Output steps (migrated from [output]) ---
[[step]]
    action = "OutputFASTQ"
    compression = "gzip"
    output_hash_uncompressed = true
    output_hash_compressed = true
    chunk_size=0
