[input]
    read1 = 'input_read1.fq'

[output]
    prefix = "output"

# --- Output steps (migrated from [output]) ---
[[step]]
    action = "OutputBAM"
    tag_to_reference = { from_bam = "input_reference.bam" }
