status: closed
# consider removing thread count all together. Needs some benchmarking, I suppose.

one core is enough, if you're bottlenecked by decompression :).


closed: the flexible work pipeline takes care of it mostly.
