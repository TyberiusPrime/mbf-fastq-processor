status: closed
# Barcode region length mismatch

Should we warn about that?
Maybe we can make it an argument to demultiplex,
'fail if barcode length mismatch'...

We warn if nothing matches. best we can do

