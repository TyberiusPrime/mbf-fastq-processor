status: closed
# Doublestage Deduplication

a) Group reads by their seq/tag/name
b) Then use a second tag (umi...) for within-group deduplication


Frankly, at that place we need to collect them all, 
then do the fancy umi dedup algorithms,
that's outside of our scope. And it's strictly inferior to alignment-based dedup anyway.
